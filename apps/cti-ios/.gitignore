# Generated by `xcodegen generate` from project.yml — never committed.
# project.yml is the single source of truth; committing its output as well
# only invites the two to drift apart.
*.xcodeproj/
App/Info.plist
App/Callsign.entitlements
CallDirectory/Info.plist
CallDirectory/CallDirectory.entitlements
# Xcode build products.
DerivedData/
build/
*.xcuserstate
.DS_Store
